# Retest Matrix

| bug_id | fix_claimed | retest_steps | result | regression_impact | pass_fail | timestamp |
|--------|-------------|--------------|--------|-------------------|-----------|-----------|
| BUG-001 | App.tsx sidebar split: scrollable primary nav + pinned bottom section (Profile/Activity Logs/Settings/Upgrade) | 1. Unlock vault at 1190x736 viewport 2. Observe sidebar without scrolling | Profile, Activity Logs, Settings, Upgrade to Pro all visible at bottom of sidebar without scroll | 73/73 e2e pass | PASS | 2026-04-07 |
| BUG-002 | App.tsx: capture location from useLocation(); add conditional `bg-accent font-semibold` active class to nav buttons | 1. Unlock vault 2. Land on Dashboard 3. Observe sidebar | Dashboard item highlighted with distinct active background | 73/73 e2e pass | PASS | 2026-04-07 |
| BUG-003 | App.tsx: PUBLIC_PATHS array + public-only Switch branch for unauthenticated users | 1. Open incognito / clear vault state 2. Navigate to /privacy 3. Navigate to /terms | Both pages render without requiring vault unlock | 73/73 e2e pass | PASS | 2026-04-07 |
| BUG-005 | index.html title; Layout.tsx header text; backend email subject | 1. Open admin.ironvault.app 2. Check browser tab title 3. Check sidebar header | Tab: "IronVault Admin Console"; Sidebar: "IronVault Admin" | Admin console independent deploy | PASS | 2026-04-07 |
| BUG-007 | Backend: flat `total` field added alongside pagination; Frontend: `??` chaining to accept both shapes | 1. Log into admin console 2. Go to Customers tab 3. Check list header | Customer List (N total) matches visible row count | Admin console independent deploy | PASS | 2026-04-07 |
| BUG-008 | admin-data.json: plan_name + subscription_plan set to "Lifetime"; total_spent set to 299 | 1. Log into admin console 2. Go to Customers 3. Find saketsuman33@gmail.com 4. Check Plan column | Plan shows "Lifetime" | Admin console independent deploy | PASS | 2026-04-07 |
| BUG-004 | Router: `!isUnlocked` branch returns `<Switch>` with `<Route path="/" component={LandingPage} />` + public info routes + catch-all landing | 1. Open incognito 2. Navigate to https://www.ironvault.app 3. Observe page content | Marketing landing page renders with hero, features, pricing, FAQ sections | 73/73 e2e pass | PASS | 2026-04-07 |
| BUG-009 | PublicPageWrapper: sets overflowY:auto on html/body/#root on mount; cleanup restores hidden on unmount | 1. Visit ironvault.app unauthenticated 2. Try to scroll 3. Click Log in 4. Unlock vault 5. Verify Dashboard | Page scrolls; Log in reaches /auth/login; after unlock lands on Dashboard with overflow:hidden restored | 73/73 e2e pass | PASS | 2026-04-07 |
| BUG-010 | SignupPage at /auth/signup with all fields (email/name/country/phone/plan/master password); post-signup routing | 1. Click Get started free on landing page 2. Fill all fields 3. Pick plan 4. Submit | Signup form appears; all fields captured; Free → Dashboard; Lifetime → /upgrade | 73/73 e2e pass | PASS | 2026-04-07 |
| BUG-011 | Two-stage auth: login.tsx accountStep = hasAccountCredentials(); Step 1: email + account password; Step 2: vault master password | 1. Signup (creates iv_account) 2. Log out 3. Click Log In 4. Enter email + account password 5. Enter master password | Step 1 appears; after correct account creds → Step 2; after master password → Dashboard | 73/73 e2e pass | PASS | 2026-04-07 |
| BUG-012 | SignupPage: added accountPassword + confirmAccountPassword fields; saveAccountCredentials(email, accountPassword) on submit | 1. Visit /auth/signup 2. Fill all fields including Account Password 3. Submit 4. Log out 5. Try Log In | Account Password and Master Password sections visible; iv_account in localStorage; two-step login works | 73/73 e2e pass | PASS | 2026-04-07 |
| BUG-013 | landing.tsx: hamburger min-h-[44px]; nav items min-h-[44px]; footer grid-cols-1 sm:grid-cols-2; hero gap-8 lg:gap-16 | 1. Visit ironvault.app on mobile (390px) 2. Check hamburger tap target 3. Open menu 4. Check item heights 5. Scroll to footer | Hamburger ≥44px; menu items ≥44px; footer stacks to 1 col at xs; hero text and CTA visible above fold | 73/73 e2e pass | PASS | 2026-04-07 |
| BUG-014 | account-auth.ts session persistence; auth-context.tsx three-tier state; NEW vault-picker.tsx; NEW create-vault.tsx; login.tsx Stage 1 only; signup.tsx Stage 1 only; App.tsx three-tier routing + sidebar groups | 1. Fresh visit → landing page (Tier 1). 2. Log In → email+account password form (Stage 1). 3. Submit → vault picker (Tier 2) with local vaults listed. 4. Enter master password → Dashboard (Tier 3). 5. Reload → vault picker shown (account session persists in localStorage). 6. Sign up → Stage 1 form → /auth/create-vault → Dashboard. 7. Account logout → landing page; session cleared. 8. Sidebar shows Vault and Finance section labels. | Landing shown when no account session; vault picker after login; correct vault unlocks with master password; account session survives page reload; vault session cleared on reload; signup creates account only then redirects to create-vault; logout clears session; sidebar has section groups | 81/81 e2e pass (8 new BUG-014 tests) | PASS | 2026-04-07 |
| TEST-MOBILE | Added prod-mobile-chrome project (Pixel 5, 393×851) to playwright.prod.config.ts; added 5 new mobile-specific tests (14.9-14.13); updated all e2e helpers and assertions to use DOM-based checks (waitForFunction / page.evaluate) for mobile overflow:hidden compatibility | 1. Run full suite with --config playwright.prod.config.ts. 2. Verify both prod-desktop-chrome and prod-mobile-chrome projects pass. 3. Confirm 14.9 (no session → landing), 14.10 (cached session → vault picker), 14.11 (cache clear → Tier 1), 14.12 (multi-vault independent passwords), 14.13 (biometric absent on web). | All 172 tests pass: 86 desktop + 86 mobile. Mobile tests 14.9-14.13 verify auth tier routing on mobile viewport. | 172/172 e2e pass | PASS | 2026-04-07 |
| FEAT-CLOUD | api/index.ts: POST /api/auth/token (trust-on-first-use JWT, legacy plan inherit) + GET/POST/PUT/DELETE/PATCH /api/vaults/cloud/* endpoints; manual HS256 JWT; plan-gated creation (free→403); last-write-wins PUT; DATABASE_URL + JWT_SECRET added to fervent-mclaren Vercel env; www.ironvault.app aliased to fervent-mclaren deployment | 1. POST /api/auth/token → 3-part JWT returned. 2. GET /api/vaults/cloud → array in vaults field. 3. POST /api/vaults/cloud → 201 (Pro+ user). 4. GET list → created vault present. 5. GET /:id → full blob returned. 6. PUT /:id → 200 merged. 7. Vault picker shows Cloud section. 8. DELETE → 200; second GET → 404. | All 16 cloud tests pass (16.1–16.8) on both desktop + mobile. Full suite 188/188. | 188/188 e2e pass | PASS | 2026-04-07 |
| BUG-015 | vault-manager.ts: email-scoped localStorage keys (ironvault_registry_<suffix>/ironvault_active_vault_<suffix>); setAccountEmail() with legacy migration; clearAccountEmail(); getActiveVaultId() stale-pointer guard; auth-context.tsx wired set/clear; vault-selection-context.tsx accountEmail dep on loadVaults; vault-picker.tsx accountEmail dep on cloud load; e2e section 17 (5 isolation tests) | 1. Login Account A → create vault. 2. Logout. 3. Login Account B → vault picker shows only B's vaults (none if B is new). 4. In-app vault selector dropdown renders and opens. 5. Cross-account: A's vault names never visible to B. 6. Logout A → login B → 0 unlock buttons. | Account A vaults never shown to B; dropdown renders with correct activeVault; fresh account shows empty state; selector shows only current-account vaults | 208/208 e2e pass | PASS | 2026-04-07 |
| BUG-016 | App.tsx: createPortal from react-dom; vault-switcher buttons use refs + toggleVaultSwitcher(); dropdown rendered to document.body at z-9999; removed overflow-hidden from mobile header | 1. Log in (desktop). 2. Click vault name button in header → dropdown renders above all content. 3. On mobile: tap vault chip → dropdown appears (not clipped). 4. Click vault in dropdown → switches vault. | Dropdown renders above all page content on both desktop and mobile; click-away closes it | 210/210 e2e pass | PASS | 2026-04-07 |
| BUG-016 (REOPENED) | App.tsx: replaced createPortal custom dropdown with Radix DropdownMenu components. Mobile header left div: min-w-0 flex-1 overflow-hidden. Right div: shrink-0. | 1. Log in with ≥2 vaults (desktop). 2. Click vault name in header → Radix dropdown opens above all content. 3. Mobile: tap vault chip → dropdown appears, right icons still visible. 4. Select vault → switches. 5. Hamburger click-away closes dropdown. | Dropdown renders above page content on both desktop + mobile; right icons always visible on mobile | PENDING post-deploy | PENDING | 2026-04-07 |
| BUG-017 | landing.tsx: Log In button (outline) added to hero CTA. login.tsx: Forgot password? link shows toast with support email. Login footer updated. | 1. Visit landing page → hero shows Get Started + Log In buttons side by side. 2. Click Log In → /auth/login. 3. On login page → see Forgot password? link next to password label. 4. Click Forgot password? → toast shown with contact email. | Log In visible in hero; Forgot password? on login page | PENDING post-deploy | PENDING | 2026-04-07 |
| BUG-018 | App.tsx mobile header: left div min-w-0 flex-1 overflow-hidden; right div shrink-0; vault chip max-w-[90px] shrink | 1. Log in on mobile (375×812). 2. Observe header. 3. Confirm notification bell + theme toggle + lock button visible on right side. 4. Vault chip truncates if long name. | All right-side icons visible on mobile; vault chip truncates instead of pushing icons off screen | PENDING post-deploy | PENDING | 2026-04-07 |
| BUG-019 | MoreSheet.tsx: container overflow-hidden flex flex-col; content div flex-1 overflow-y-auto overscroll-contain | 1. Open app on mobile. 2. Tap hamburger/More button. 3. Scroll the menu. 4. Confirm Settings and Profile items reachable at bottom. | Menu scrollable; Settings and Profile visible and tappable | PENDING post-deploy | PENDING | 2026-04-07 |
| BUG-020 | auto-lock.ts: gracePeriodMs default 0 → 30000; loadSettings default 0 → 30000; web visibilitychange: no immediate lock; resetIdleTimer on tab visible | 1. Unlock vault. 2. Navigate to /profile. 3. Wait 2 minutes without interaction. 4. On mobile: dim/wake screen. 5. Switch tabs and return. | App stays on /profile; no redirect to Dashboard; no flicker; tab-hide+show within 30s does not lock; tab-hide+show after 30s locks gracefully | PENDING post-deploy | PENDING | 2026-04-07 |
| BUG-021 | NEW plans.ts canonical source; pricing.tsx/signup.tsx/landing.tsx/customer-info-dialog.tsx/profile.tsx/admin-console updated | 1. View /pricing → 4 plans with INR prices, Lifetime column present. 2. View /auth/signup → same plan names and prices. 3. View landing pricing section → Pro Monthly ₹149/mo, Pro Family ₹299/mo. 4. Log in → view profile → plan shows canonical name and INR price. 5. Admin console → plans list matches. | Identical plan names + prices across all views: Free / Pro Monthly ₹149/mo / Pro Family ₹299/mo / Lifetime ₹9,999 | PENDING post-deploy | PENDING | 2026-04-07 |
| BUG-022 | plans.ts localVaultLimit; vault-manager.ts PLAN_LIMIT error; usePlanFeatures hook; create-vault upgrade banner; vault-picker upgrade CTA | 1. Free user with 1 vault: vault-picker shows upgrade prompt instead of Add button. 2. Free user navigates to /auth/create-vault: sees amber upgrade banner, submit disabled. 3. Pro user: can create vaults 1–5 normally. 4. At vault 5: upgrade prompt shown. 5. usePlanFeatures() cache refreshes after 5 min. | Free user blocked at 1 vault with clear upgrade CTA; Pro user allowed up to 5 | PENDING post-deploy | PENDING | 2026-04-07 |
| BUG-023 | api/index.ts /api/crm/migrate; family_invites + plan_audit_log tables; vault_count + flagged_over_limit columns | 1. POST /api/crm/migrate → 200 schema migration complete. 2. GET /api/crm/family-invites/:email → empty array. 3. POST /api/crm/family-invites → invite created. 4. PATCH /api/crm/family-invites/:id status=revoked → invite revoked. 5. POST /api/crm/vaults/report → vault_count updated, flagged if over limit. | Migration succeeds; family invites CRUD works; vault count reported and flagged correctly | PENDING post-deploy | PENDING | 2026-04-07 |
| BUG-024 | admin-console/backend/server-simple-working.ts (deployed backend): added GET /api/customers/:id/vaults endpoint. Note: api/index.ts endpoints were not deployed — server-simple-working.ts is the actual admin backend. | 1. GET https://admin.ironvault.app/api/customers/1/vaults → returns { customer_id, email, vault_count, plan }. | vault_count returned based on plan + vault_created flag; endpoint no longer returns 404 | 494/494 e2e pass post-deploy | PASS | 2026-04-08 |
| BUG-025 | server/routes.ts: /api/crm/entitlement/:userId now accepts email (if contains @, resolves via getCrmUserByEmail). Also adds top-level `plan` field to response so usePlanFeatures hook can read data.plan correctly. | 1. Log in. 2. Network: GET /api/crm/entitlement/:email returns { plan: "lifetime", entitlement: { plan: "lifetime" } }. 3. usePlanFeatures() returns isPaid: true, isLifetime: true. 4. Vault creation allows up to 5 vaults. | Hook reads correct plan from server; plan gating reflects actual entitlement within 5 min | 494/494 e2e pass post-deploy | PASS | 2026-04-08 |
| BUG-026 | expenses.tsx/subscriptions.tsx/api-keys.tsx/bank-statements.tsx/documents.tsx/goals.tsx/investments.tsx: ALL hooks moved above UpgradeGate guard; guard at bottom before JSX return; licenseLoading check prevents flash | 1. Log in as pro user. 2. Navigate to /expenses, /subscriptions, /api-keys, /bank-statements, /documents, /goals, /investments. 3. Click Add/Edit/Delete buttons on each page. 4. Repeat as free user: see UpgradeGate, not broken page. | All buttons functional for pro user; UpgradeGate shows correctly for free user; no hooks violation crash | 264/264 e2e pass | PASS | 2026-04-08 |
| BUG-027 | api/index.ts /api/crm/entitlement/:id returns `{ ...flat, entitlement: { ...flat } }` for both found and not-found cases | 1. Log in as pro user. 2. Navigate to /expenses. 3. Verify UpgradeGate NOT shown. 4. Check network: GET /api/crm/entitlement/:id returns { plan: 'lifetime', entitlement: { plan: 'lifetime' } } | Pro user sees pro content; entitlement response has both flat and nested shapes | 264/264 e2e pass | PASS | 2026-04-08 |
| BUG-028 | license-context.tsx: useEffect([isUnlocked]) — when vault unlocks: reset hasSyncedFromServer.current=false, call loadLicense().then(syncFromServer) | 1. Load app fresh (vault locked). 2. Enter master password → vault unlocks. 3. Navigate to /expenses. 4. Verify pro features available immediately. | Pro user sees pro content after vault unlock; no UpgradeGate after unlock | 264/264 e2e pass | PASS | 2026-04-08 |
| BUG-029 | notes.tsx button wrapper div: added `[@media(hover:none)]:opacity-100` to className | 1. Open /notes on mobile (390px or iphone-15-pro). 2. Observe note cards. 3. Edit/pin/delete buttons visible without hover. 4. Tap edit → edit modal opens. 5. Tap delete → delete confirmation. | Edit, pin, delete buttons always visible on touch devices; CRUD works on mobile | 160/160 deep-verify pass | PASS | 2026-04-08 |
| BUG-030 | settings.tsx: showClearDataDialog state; handleClearData → setShowClearDataDialog(true); handleClearDataConfirmed does actual clear; Dialog component with Cancel + destructive confirm | 1. Navigate to /settings. 2. Find Clear All Data button. 3. Click → React Dialog appears (not window.confirm). 4. Click Cancel → dialog closes, no data cleared. 5. Click Confirm → data cleared. | React Dialog appears instead of window.confirm; Cancel dismisses; Confirm clears data | 160/160 deep-verify pass | PASS | 2026-04-08 |
| BUG-031 | notes.tsx: deleteNoteTarget state; handleDeleteNote sets target; handleDeleteNoteConfirmed does actual delete; Dialog with data-testid=dialog-delete-note and button-confirm-delete-note | 1. Navigate to /notes. 2. Click delete on a note. 3. React Dialog appears. 4. Click Cancel → no delete. 5. Click Delete → note removed. | React Dialog appears instead of window.confirm; Cancel/Confirm both work | Manual QA verified 2026-04-08 | PASS | 2026-04-08 |
| BUG-032 | reminders.tsx: deleteReminderTarget state; handleDelete sets target; handleDeleteReminderConfirmed does actual delete; Dialog with data-testid=dialog-delete-reminder and button-confirm-delete-reminder | 1. Navigate to /reminders. 2. Click delete on a reminder. 3. React Dialog appears. 4. Click Cancel → no delete. 5. Click Delete → reminder removed. | React Dialog appears instead of window.confirm; Cancel/Confirm both work | Manual QA verified 2026-04-08 | PASS | 2026-04-08 |
| BUG-033 | auth-context.tsx accountLogin() calls POST /api/auth/token as primary; falls back to localStorage on network error. initializeAuth() syncs hash on session restore. | 1. Open incognito window 2. Go to https://www.ironvault.app/auth/login 3. Enter saketsuman1312@gmail.com + account password 4. Observe login succeeds 5. Vault picker appears | Login succeeds from a browser with no prior localStorage; vault picker shown | 454 e2e pass | PENDING — requires PR#1 merge to main | 2026-04-09 |
